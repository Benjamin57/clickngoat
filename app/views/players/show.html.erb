<div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.2), rgba(0,0,0,0.3)), url('<%= cl_image_path @player.photo %>');">
  <div class="banner-content">
    <h1><%= @player.name %></h1>
    <h2><%= @player.level %></h2>
  </div>
</div>
<div class="container">
  <div class="row" >
    <div class="col-md-8">
      <div class="player-show-card">
        <p><%= @player.city %></p>
        <h3>Description</h3>
        <p><%= @player.description %></p>
      </div>
    </div>
    <% if @player.user != current_user %>
      <div class="col-md-4">
        <div class="player-show-card">
        <h4><%= number_with_delimiter(@player.price_per_day, :delimiter => ',') %>€ per day</h4>
        <%= simple_form_for [@player, @booking] do |f| %>
          <div class="form-wrapper" style="margin: 0 auto;">
            <%= f.input :pick_your_date, as: :string, required: false, input_html: {id: "range_start"} %>
            <%= f.submit "Book", class: "btn-yellow" %>
          </div>
        <% end %>
      </div>
    </div>
    <% end %>
  </div>

<!-- Calculer average_rating -->
<!-- sum of reviews / count reviews  -->

  <div class="row">
    <div class="col-md-8">
      <div class="player-show-card">
        <h3>Reviews</h3>
        <div class="list-group">
          <li class="list-group-item" id="reviews" >
            <% @player.reviews.each do |review|  %>
              <%= render "reviews/show", review: review %>
            <% end %>

          </li>
        </div>
      </div>
      <% if @player.user != current_user %>
        <div class="player-show-card">
          <h3>Add a new Review</h3>
          <%= render "reviews/form", player: @player, review: @review %>
        </div>
      <% end %>
    </div>
  </div>
</div>

           <%= f.input :content %>
           <h5><strong>Rating *</strong></h5>
           <%= f.input :rating, as: :hidden %>
           <i class="far fa-star js-star" id="star1" data-value="1"></i>
           <i class="far fa-star js-star" id="star2" data-value="2"></i>
           <i class="far fa-star js-star" id="star3" data-value="3"></i>
           <i class="far fa-star js-star" id="star4" data-value="4"></i>
           <i class="far fa-star js-star" id="star5" data-value="5"></i>
           <br>
           <%= f.submit "Add a review", class: "btn-white" %>
         <% end %>
       </div>
     <% end %>
    </div>
  </div>
</div>



